<!-- File: views/admin/brandForm.ejs -->
<% title = brand ? 'Edit Brand' : 'Add Brand' %>

<div class="container py-4">
  <div class="row justify-content-center">
    <div class="col-12 col-md-6">
      <h1 class="mb-4 text-center"><%= title %></h1>
      <form action="<%= brand ? '/admin/brands/' + brand.id + '?_method=PUT' : '/admin/brands' %>" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label">Name</label>
          <input
            type="text"
            id="name"
            name="name"
            class="form-control"
            required
            value="<%= brand ? brand.name : '' %>"
          >
        </div>
        <div class="d-flex justify-content-center">
          <button type="submit" class="btn btn-primary me-2">
            <%= brand ? 'Update' : 'Add' %> Brand
          </button>
          <a href="/admin/brands" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</div>

